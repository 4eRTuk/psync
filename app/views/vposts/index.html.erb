<% provide(:title, "Все посты (стр.#{params[:page]})") %>

<% if @vposts.count > 0 %>
	<h4>Страница <%= params[:page] %></h4>
<% end %>
<%= will_paginate @vposts %>

<ul class="vpost-list c">
  <%= render @vposts %>
</ul>

<%= will_paginate @vposts %>